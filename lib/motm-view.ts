import { noStoreHeaders } from "./discord-auth";

export const esc = (value: unknown) => String(value ?? "").replace(/[&<>"']/g, c => ({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c]!));
export const formatKickoff = (value: string | Date) => new Intl.DateTimeFormat("nl-NL", { weekday:"long", day:"numeric", month:"long", hour:"2-digit", minute:"2-digit", timeZone:"Europe/Amsterdam" }).format(new Date(value));
export const page = (title: string, body: string, script = "") => new Response(`<!doctype html><html lang="nl"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="robots" content="noindex,nofollow"><meta name="theme-color" content="#07080c"><title>${esc(title)} | AjaxPro</title><link rel="icon" href="/Favicon/favicon.ico"><link rel="stylesheet" href="/motm.css?v=1"></head><body><header class="motm-header"><a href="/club"><img src="/assets/ajaxpro-logo.png" alt="AjaxPro"><span>Club</span></a></header>${body}${script ? `<script src="${script}" defer></script>` : ""}</body></html>`, { headers: { ...noStoreHeaders, "Content-Type":"text/html; charset=UTF-8", "Content-Security-Policy":"default-src 'none'; style-src 'self'; img-src 'self' https://cdn.discordapp.com; script-src 'self'; connect-src 'self'; form-action 'self'; base-uri 'none'; frame-ancestors 'none'" } });
export const errorPage = (title: string, message: string, status = 400) => { const response = page(title, `<main class="motm-main"><section class="empty"><p class="eyebrow">Man of the Match</p><h1>${esc(title)}</h1><p>${esc(message)}</p><a class="button" href="/club">Terug naar Club</a></section></main>`); return new Response(response.body, { status, headers: response.headers }); };
export const matchHeading = (match: any) => `<p class="eyebrow">${esc(match.competition)} · ${match.home_or_away === "home" ? "Thuis" : "Uit"}</p><h1>Ajax <span>—</span> ${esc(match.opponent)}</h1><p class="match-date">${esc(formatKickoff(match.kickoff_at))}${match.home_score != null ? ` · ${match.home_score}–${match.away_score}` : ""}</p>`;
